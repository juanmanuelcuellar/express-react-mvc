extends ../../config/config.pug

block content

    #wrapper.d-flex
        include ../../listgroup/listgroup-admin.pug
        #page-content-wrapper
            include ../../Navbars/navbar-sidebar.pug
            .container-fluid
                h1.mt-4 empleados

    script.
        $('#menu-toggle').click(function(e){
            e.preventDefault();
            $('#wrapper').toggleClass('toggled');
        });